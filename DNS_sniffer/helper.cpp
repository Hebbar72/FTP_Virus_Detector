#include<iostream>
#include<iomanip>
#include<climits>

using namespace std;

struct helper
{
    int data;
};



int main(int argc, char* args[])
{
    // int m = atoi(args[1]);
    // int n = atoi(args[2]);
    // int** arr = (int**)malloc(m*sizeof(int*) + m*n*sizeof(int));

    // for(int i = 0;i < m;i++)
    //     *(arr + i) = (int*)(arr + m) + i*n;
    
    // for(int i = 0;i < m;i++)
    // {
    //     for(int j = 0;j < n;j++)
    //     { 
    //         arr[i][j] = i*n + j;
    //     }
    // }

    // int a = 5, b = 10, c = 15;
    // int*** helper = (int***)malloc(a*sizeof(int**));

    // for(int i = 0;i < a;i++)
    //     helper[i] = (int**)malloc(b*sizeof(int*));
    
    // for(int i = 0;i < a;i++)
    // {
    //     for(int j = 0;j < b;j++)
    //         helper[i][j] = (int*)malloc(sizeof(int)*c);
    // }

    // int count = 0;
    // for(int i = 0;i < a;i++)
    // {
    //     for(int j = 0;j < b;j++)
    //     {
    //         for(int k = 0;k < c;k++)
    //             count++;
    //     }
    // }
    // cout << count << "\n";

    

    int x = 0;


}